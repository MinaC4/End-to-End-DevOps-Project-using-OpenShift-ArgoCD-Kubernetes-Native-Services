apiVersion: v1
kind: ResourceQuota
metadata:
  name: netflix-clone-quota
  namespace: netflix-clone
spec:
  hard:
    requests.cpu: "1"
    requests.memory: "1Gi"
    limits.cpu: "2"
    limits.memory: "2Gi"
    pods: "10"
    configmaps: "10"
    secrets: "20"
    services: "10"
